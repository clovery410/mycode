class Solution(object):
    def dfs(self, a_start, a_end, b_start, b_end, num):
        
    def isAdditiveNumber(self, num):
        length = len(num)
        dp = [False for x in xrange(length + 1)]
        dp[0] = True

        for i in xrange(length):
            j = i
            while j >= 0:
                
